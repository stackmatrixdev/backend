import express from "express";

const router = express.Router();

// Program routes - to be implemented
// router.get('/', getAllPrograms);
// router.get('/featured', getFeaturedPrograms);
// router.get('/search', searchPrograms);
// router.get('/:id', getProgramById);
// router.post('/', authenticate, authorize('admin', 'instructor'), createProgram);
// router.put('/:id', authenticate, authorize('admin', 'instructor'), updateProgram);
// router.delete('/:id', authenticate, authorize('admin'), deleteProgram);
// router.get('/:id/quizzes', getProgramQuizzes);
// router.post('/:id/enroll', authenticate, enrollInProgram);

export default router;
