import express from "express";
// Import controllers here

const router = express.Router();

// Quiz routes - to be implemented
// router.get('/', getAllQuizzes);
// router.get('/:id', getQuizById);
// router.post('/', authenticate, authorize('admin', 'instructor'), createQuiz);
// router.put('/:id', authenticate, authorize('admin', 'instructor'), updateQuiz);
// router.delete('/:id', authenticate, authorize('admin'), deleteQuiz);
// router.get('/program/:programId', getQuizzesByProgram);
// router.post('/:id/start', authenticate, startQuiz);
// router.get('/:id/statistics', getQuizStatistics);

export default router;
