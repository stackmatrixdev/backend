import express from "express";

const router = express.Router();

// Question routes - to be implemented
// router.post('/', authenticate, authorize('admin', 'instructor'), createQuestion);
// router.get('/quiz/:quizId', getQuestionsByQuiz);
// router.get('/:id', getQuestionById);
// router.put('/:id', authenticate, authorize('admin', 'instructor'), updateQuestion);
// router.delete('/:id', authenticate, authorize('admin'), deleteQuestion);
// router.post('/bulk', authenticate, authorize('admin', 'instructor'), createBulkQuestions);

export default router;
