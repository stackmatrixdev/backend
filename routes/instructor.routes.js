import express from "express";

const router = express.Router();

// Instructor routes - to be implemented
// router.post('/apply', applyAsInstructor);
// router.get('/', getAllInstructors);
// router.get('/:id', getInstructorById);
// router.put('/:id', authenticate, authorize('admin', 'instructor'), updateInstructor);
// router.put('/:id/approve', authenticate, authorize('admin'), approveInstructor);
// router.put('/:id/reject', authenticate, authorize('admin'), rejectInstructor);
// router.get('/:id/programs', getInstructorPrograms);
// router.get('/:id/stats', getInstructorStats);

export default router;
